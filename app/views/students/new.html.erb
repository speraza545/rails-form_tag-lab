<h1> Student Form </h1>

<%= form_tag students_path do %>
    <%= text_area_tag :'student[first_name]' %>
    <%= text_area_tag :'student[last_name]'%>
    <%= submit_tag "Submit Student" %>
<% end %>

<%= session[:form_params] %>